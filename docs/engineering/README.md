# Engineering Docs

这里放当前仍然有效的工程文档。

## 当前保留原则

- `specs/`：当前仍可作为实现或维护依据的设计文档
- `plans/`：只放正在执行或即将执行的实施计划
- `runbooks/`：运行、部署、排障类手册

## 归档原则

以下内容在完成后应移到 `docs/archive/engineering/`：

- 已完成的实施计划
- 已执行完毕的仓库重组 / 迁移计划
- 只具备历史追溯价值的阶段性设计稿
